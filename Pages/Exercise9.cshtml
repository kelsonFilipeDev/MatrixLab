@page
@model MatrixLab.Pages.Exercise9Model
@{
    ViewData["Title"] = "Exercício 9";
}

<div class="exercise-page">
    <a href="/" class="btn-back">← Voltar</a>
    <h2>Exercício 9</h2>
    <p class="exercise-desc">Matriz de vendas por mês e produto — total por mês e total anual por produto.</p>

    <form method="post">
        <table class="sales-table">
            <thead>
                <tr>
                    <th>Mês</th>
                    @for (int j = 0; j < 5; j++)
                    {
                        <th>Produto @(j + 1)</th>
                    }
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < 12; i++)
                {
                    <tr>
                        <td class="month-label">@Model.Months[i]</td>
                        @for (int j = 0; j < 5; j++)
                        {
                            <td>
                                <input type="number" name="Matrix[@i][@j]" value="@Model.Matrix[i][j]" class="matrix-cell" />
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>

        <div class="btn-group">
            <button type="submit" asp-page-handler="Generate" class="btn-secondary">🔀 Gerar Aleatório</button>
            <button type="submit" asp-page-handler="Execute" class="btn-primary">▶ Executar</button>
        </div>
    </form>

    @if (Model.Result != null)
    {
        <div class="result-box">
            <h3>Total por Mês</h3>
            <table class="sales-table">
                <thead>
                    <tr><th>Mês</th><th>Total</th></tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < 12; i++)
                    {
                        <tr>
                            <td class="month-label">@Model.Months[i]</td>
                            <td class="result-cell">@Model.Result.MonthTotals[i]</td>
                        </tr>
                    }
                </tbody>
            </table>

            <h3 style="margin-top:2rem">Total Anual por Produto</h3>
            <table class="sales-table">
                <thead>
                    <tr><th>Produto</th><th>Total Anual</th></tr>
                </thead>
                <tbody>
                    @for (int j = 0; j < 5; j++)
                    {
                        <tr>
                            <td class="month-label">Produto @(j + 1)</td>
                            <td class="result-cell">@Model.Result.ProductTotals[j]</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
